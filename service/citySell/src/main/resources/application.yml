spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/citysell?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

# mapper.xml文件位置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com.yuwen.citysell.dao: debug

server:
  port: 9000